export interface MemberInfo{
    sid?: string,
    name?: string ,
    gender?: string,
    phone?: string,

    school_code ?: string,
    college_code ?: string,
    major_code ?: string,

    school_name ?: string,
    college_name ?: string,
    major_name ?: string,

    score?: string,
    totalcheck?: number,
    hascheck?: number,
}